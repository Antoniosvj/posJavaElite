package com.antoniosvj;

public class TesteString {
    void main(){
        /*
        String nome = "Antonio Sérgio Viana Júnior";

        IO.println(nome.toUpperCase()); //tudo maiusculo
        IO.println(nome.toLowerCase()); //tudo minusculo
        IO.println(nome.length()); //conta as letras
        IO.println(nome.charAt(0)); //retornar o caracter na primeira posição
        IO.println(nome.charAt(1));
        IO.println(nome.toLowerCase().replace(" ", "_")); //troca os espaços por _
        IO.println(nome.contains("Ant")); //retorna um boolean se contain ou não .... é case sensitive
        IO.println(nome.contains("lora"));
        IO.println(nome.startsWith("Antonio")); //o nome começa com Antonio - case sensitive
        IO.println(nome.endsWith("Antonio")); //o nome termina com Júnior - case sensitive
        IO.println("O nome é ".concat(nome)); //concatenando uma string
        IO.println(nome.concat(" teste")); //concatenando uma string
        IO.println(nome.substring(0,7)); // imprime o caracter do 0 ao 6 (é exclusivo)
        IO.println(nome.substring(5,15));

        String[] pedacos = nome.split(" "); //divide a string nos espaços
        IO.println(pedacos.length);
        for (String pedaco:pedacos) IO.println(pedaco); //imprime cada pedaço

        String nomeDigitado = IO.readln("Digite um nome: ");
        IO.println(nome.equals(nomeDigitado)); //compara se o nome digitado é igual ao que estava em memoria -> case sensitive
        IO.println(nome.equalsIgnoreCase(nomeDigitado)); //ignore maiusculas e minusculas

        nome = nome.toUpperCase();
        IO.println(nome);

        long inicioSB = System.currentTimeMillis();
        StringBuilder builder = new StringBuilder();
        for (int i = 0; i < 1_000_000; i++){
            builder.append(i).append(", ");
        }
        long fimSB = System.currentTimeMillis();
        IO.println(fimSB - inicioSB);

         */
    }
}
